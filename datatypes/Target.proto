syntax = "proto3";

package MRA.Datatypes;

import "datatypes/Pose.proto";

message Target
{
    Pose position = 1;
    Pose velocity = 2;
}

// Can be used in two phases of robot setpoint processing:
// 1. Input for PathPlanning: go to target setpoint (FCS) while avoiding collisions
// 2. Output of PathPlanning, input for VelocityControl: determine robot (wheel) speed setpoint(s) based on given "safe" target setpoint (RCS?)

