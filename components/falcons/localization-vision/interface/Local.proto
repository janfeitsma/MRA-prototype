syntax = "proto3";

package MRA.FalconsLocalizationVision;

import "datatypes/CvMat.proto";

message Local
{
    // optional debug Mat, for plotting, only set when params.debug is enabled
    MRA.Datatypes.CvMatProto fitResultFloor = 1;
}

